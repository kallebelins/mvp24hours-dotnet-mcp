# Kubernetes Service for Mvp24Hours Sample Application
apiVersion: v1
kind: Service
metadata:
  name: mvp24hours-api
  labels:
    app: mvp24hours-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: mvp24hours-api
