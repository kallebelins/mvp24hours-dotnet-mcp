# Kubernetes Secret for Mvp24Hours Sample Application
# IMPORTANT: In production, use external secrets management (Azure Key Vault, AWS Secrets Manager, etc.)
# These are example values only - DO NOT commit real secrets!
apiVersion: v1
kind: Secret
metadata:
  name: mvp24hours-secrets
type: Opaque
stringData:
  # Replace with actual connection strings in production
  db-connection-string: "Server=sqlserver;Database=mvp24hours_db;User Id=sa;Password=CHANGE_ME;TrustServerCertificate=true"
  redis-connection-string: "redis:6379,password=CHANGE_ME"
  rabbitmq-connection-string: "amqp://mvp24hours:CHANGE_ME@rabbitmq:5672/"
  jwt-secret: "your-super-secret-jwt-key-at-least-32-characters-long"
